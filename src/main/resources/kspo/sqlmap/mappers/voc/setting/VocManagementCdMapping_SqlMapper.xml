<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kspo.voc.kspo.setting.dao.VocManagementCdMappingDao">

    <select id="select" resultType="com.kspo.voc.kspo.setting.model.VocManagementCdMappingVo">
        select
            *
        from
            TB_VOC_MANAGEMENT_CD_REL tvmcr inner join TB_VOC_MANAGEMENT_CD_BAS tvmcb
            on tvmcr.management_cd = tvmcb.management_cd
        <where>
            <if test="mappingSeq != null and !mappingSeq.equals('')">
                mapping_seq = #{mappingSeq}
            </if>
            <if test="managementCd != null and !managementCd.equals('')">
                and
                management_cd = #{managementCd}
            </if>
        </where>
    </select>

    <select id="selectList" resultType="com.kspo.voc.kspo.setting.model.VocManagementCdMappingVo">
        select
            *
        from
            TB_VOC_MANAGEMENT_CD_REL tvmcr inner join TB_VOC_MANAGEMENT_CD_BAS tvmcb
            on tvmcr.management_cd = tvmcb.management_cd
        <where>
            <if test="prntsMappingSeq != null and !prntsMappingSeq.equals('')">
                and
                prnts_mapping_seq = #{prntsMappingSeq}
            </if>
        </where>
    </select>

    <select id="selectMaxCd" resultType="String">
        select
            max(mapping_seq)
        from
            TB_VOC_MANAGEMENT_CD_REL
    </select>

    <insert id="insert">
        insert into TB_VOC_MANAGEMENT_CD_REL
        (MAPPING_SEQ, MANAGEMENT_CD, PRNTS_MAPPING_SEQ, LVL, REGR_ID)
        values
            (
             #{mappingSeq},
             #{managementCd},
             #{prntsMappingSeq},
             #{lvl},
             #{loginUsr}
            )
    </insert>
    
    <delete id="delete">
        delete from TB_VOC_MANAGEMENT_CD_REL
        where
            mapping_seq in
            <foreach collection="list" item="vo" open="(" separator="," close=")">
                #{vo.mappingSeq}
            </foreach>
    </delete>
</mapper>
